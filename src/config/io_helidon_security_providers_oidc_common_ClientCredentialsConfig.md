Type:
[io.helidon.security.providers.oidc.common.ClientCredentialsConfig](https://helidon.io/docs/v4/apidocs/io.helidon.security.providers.oidc.common/io/helidon/security/providers/oidc/common/ClientCredentialsConfig.html)

# Configuration options

| key | type | default value | description |
|----|----|----|----|
| `scope` | string |  | Scope used when obtaining access token in the client credentials flow. It is required to set when `server-type` is set to `idcs`. |

Optional configuration options
